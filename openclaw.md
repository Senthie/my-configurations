# openclaw use cil

# create a new agent 

```bash
# create a agent
openclaw agents add <agent_name>

# need use `send` send one message by first
 openclaw agent --agent <agent_name> --message "hellow "
```
